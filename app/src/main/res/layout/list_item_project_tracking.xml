<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:bind="http://schemas.android.com/apk/res-auto"
        tools:context="com.lecet.app.content.ProjectTrackingListActivity">

    <data>
        <variable name="viewModel" type="com.lecet.app.viewmodel.ListItemProjectTrackingViewModel"/>
        <import type="android.view.View"/>
    </data>

    <LinearLayout android:orientation="vertical"
                  android:layout_width="match_parent"
                  android:layout_height="wrap_content"
                  android:layout_marginLeft="6dp"
                  android:layout_marginRight="6dp"
                  android:layout_marginTop="6dp"
                  android:paddingBottom="6dp"
                  android:background="@drawable/shape_rect_list_item_bg_1">

        <LinearLayout
                android:orientation="horizontal"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_marginTop="-4dp"
                android:gravity="center_vertical"
                android:onClick="@{viewModel.onClick}">

            <!-- Map Image -->
            <ImageView
                    android:id="@+id/project_locatation_image_view"
                    android:layout_width="60dp"
                    android:layout_height="60dp"
                    android:layout_marginTop="6dp"
                    android:layout_marginLeft="10dp"
                    bind:imageUrl="@{viewModel.mapUrl}"/>

            <!-- Details -->
            <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:paddingLeft="12dp">

                <TextView
                        android:id="@+id/project_name"
                        android:paddingTop="14dp"
                        tools:text="Parks Building"
                        style="@style/ListItemProjectTrackingTextView.Title"
                        bind:typeface="@{`Lato-Regular.ttf`}"
                        android:text="@{viewModel.projectName}"/>

                <!-- Location -->
                <LinearLayout
                        android:layout_width="match_parent"
                        android:layout_height="22dp"
                        android:orientation="horizontal">

                    <ImageView
                            android:layout_width="9dp"
                            android:layout_height="13dp"
                            android:layout_marginTop="4dp"
                            android:layout_marginRight="2dp"
                            android:src="@mipmap/ic_bid_location"/>

                    <TextView
                            android:id="@+id/location"
                            tools:text="Tampa, FL"
                            style="@style/ListItemProjectTrackingTextView.Detail1"
                            bind:typeface="@{`Lato-Bold.ttf`}"
                            android:text="@{viewModel.clientLocation}"/>

                </LinearLayout>

                <!-- Keywords / Tags -->
                <LinearLayout
                        android:layout_width="match_parent"
                        android:layout_height="20dp"
                        android:orientation="horizontal">

                    <ImageView
                            android:layout_width="11dp"
                            android:layout_height="16dp"
                            android:layout_marginTop="2dp"
                            android:layout_marginRight="2dp"
                            android:src="@drawable/tag_icon"/>

                    <TextView
                        android:id="@+id/keywords"
                        tools:text="Vehicle Sales/Service"
                        style="@style/ListItemProjectTrackingTextView.Detail2"
                        bind:typeface="@{`Lato-Bold.ttf`}"
                        android:text="@{viewModel.projectKeywords}"/>

                </LinearLayout>

            </LinearLayout>

        </LinearLayout>

        <!-- Vertical Layout for Optional Expandable Bid & Note Views -->
        <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="vertical"
                android:visibility="gone"
                android:layout_marginTop="8dp">

            <!-- Expandable Bid View -->
            <RelativeLayout
                    android:id="@+id/expandable_layout_bids"
                    android:layout_width="match_parent"
                    android:layout_height="40dp"
                    android:paddingTop="10dp"
                    android:paddingLeft="8dp"
                    android:orientation="horizontal"
                    android:background="@drawable/shape_rect_list_item_expand_1"
                    android:visibility="@{viewModel.hasBid ? View.VISIBLE : View.GONE}"
                    android:onClick="@{viewModel.onBidLayoutClick}">

                <ImageView
                        android:id="@+id/add_icon"
                        android:layout_width="14dp"
                        android:layout_height="14dp"
                        android:layout_marginRight="4dp"
                        android:layout_gravity="center_vertical"
                        android:src="@mipmap/ic_add"/>

                <TextView
                        android:id="@+id/expandable_text_bid"
                        tools:text="A new bid has been placed"
                        style="@style/ListItemProjectTrackingTextView.Expandable"
                        android:layout_toRightOf="@id/add_icon"
                        android:layout_height="wrap_content"
                        bind:typeface="@{`Lato-Bold.ttf`}"
                        android:text="@{viewModel.projectKeywords}"/>

                <ImageView
                        android:layout_width="11dp"
                        android:layout_height="16dp"
                        android:layout_marginRight="12dp"
                        android:layout_alignParentRight="true"
                        android:src="@mipmap/ic_caret_down"/>

            </RelativeLayout>

            <!-- Expandable Note View -->
            <RelativeLayout
                    android:id="@+id/expandable_layout_notes"
                    android:layout_width="match_parent"
                    android:layout_height="40dp"
                    android:paddingTop="10dp"
                    android:paddingLeft="8dp"
                    android:orientation="horizontal"
                    android:background="@drawable/shape_rect_list_item_expand_1"
                    android:visibility="@{viewModel.hasNote ? View.VISIBLE : View.GONE}"
                    android:onClick="@{viewModel.onNoteLayoutClick}">

                <ImageView
                        android:id="@+id/note_icon"
                        android:layout_width="14dp"
                        android:layout_height="14dp"
                        android:layout_marginRight="4dp"
                        android:layout_gravity="center_vertical"
                        android:src="@mipmap/ic_add_note"/>

                <TextView
                        android:id="@+id/expandable_text_note"
                        tools:text="A new note has been added"
                        style="@style/ListItemProjectTrackingTextView.Expandable"
                        android:layout_toRightOf="@id/note_icon"
                        android:layout_height="wrap_content"
                        android:layout_gravity="center_vertical"
                        bind:typeface="@{`Lato-Bold.ttf`}"
                        android:text="@{viewModel.projectNote}"/>

                <ImageView
                        android:layout_width="11dp"
                        android:layout_height="16dp"
                        android:layout_marginRight="12dp"
                        android:layout_alignParentRight="true"
                        android:src="@mipmap/ic_caret_down"/>

            </RelativeLayout>

        </LinearLayout>

    </LinearLayout>

</layout>